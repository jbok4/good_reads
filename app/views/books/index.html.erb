<p id="notice"><%= notice %></p>
<% if !user_signed_in? %>
  <div class="jumbotron">
    <h1>All the latest book reviews</h1>
    <p>Site Subtitle</p>
    <p><%= link_to "Sign Up Now", new_user_registration_path, class: "btn btn-primary btn-lg" %></p>
  </div>
<% end %>
<div class="row">
  <% @books.each do |book| %>
    <div class="col-sm-6.col-md-3">

    </div>

    <% end %>
</div>