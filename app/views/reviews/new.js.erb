// Links with write a review link on book show page 
// renders form partial and escapes the html with js

  // $("#book_review").hide().after('<%= j render("form") %>');

// var form = $('<%= j render("form") %>');
// var wrapper = $('<div>').attr('id', 'book_review').append(form);
// $('place-here').append(wrapper)

function Item(attributes){
  this.comment = attributes.comment;
  this.id = attributes.id;
}

Item.prototype.renderLI = function(){
  
}

$(function(){
  $('#new_review').on('submit', function(e) {
    e.preventDefault()
    // console.log('hello')
    var $form = $(this);
    var action = $form.attr("action");
    var params = $form.serialize();

    $.ajax({
        url: action,
        data: params,
        dataType: "json",
        method: "POST",
        success: function(json) {
        //append to review list the new data that is returned
    var item = new Item(json);
      }
    })
  $('.review_title').append(html)
  })
})